
<h3 class="fluid-text monolisk" id="molds101">MOLDS Overview</h3>
<p class="padded warning bitstream b-s-t"><strong>NOTE:</strong> <em>MOLDS</em> will be available in 3.1, which will introduce the <strong>BOS</strong> portal.</p>
<p class="bitstream" style="margin-top: 1.5rem;">MOLDS are modular content objects that are linked to data source files that are managed via the <a href="#lockedout" data-modal-open class="lemonlink">BOS Portal</a>.</p>
<div class="flex bitstream"><div class="padded half">
	<h4 class="flow-text charcoal">Module Data Management</h4>
	<p>Data for various types of MOLDS (modules) is managed via <strong>BOS</strong>, which stores data as XML, CSV, or plain text files. <em>No database needed.</em> This data is then used to construct objects that can be rendered on the front-end of your application / website.</p>
</div><div class="padded half">
	<h4 class="flow-text charcoal">Using MOLDS</h4>
	<p>MOLDS are content modules or data sources that are rendered on the front-end. Simply include the <em>view.php</em> file for any given MOLD (module), and echo the necessary PHP variable(s) to render the module. Further documentation is provided with each module object.</p>
</div></div>